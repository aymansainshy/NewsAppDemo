import 'package:flutter/material.dart';

class PageModel {
  IconData icons;
  String images;

  PageModel(this.icons, this.images);

//   IconData get icons => _icons;
//   String get images => _images;
}
